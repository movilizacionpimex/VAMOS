# ============================================================
#  NETLIFY CONFIGURATION — VAMOS
#  Este archivo configura el comportamiento del sitio en Netlify
# ============================================================

# Build settings
[build]
  publish = "."        # La raíz del proyecto es lo que se despliega

# Nota: Se eliminó la regla [[redirects]] para /admin → /admin/
# porque causaba conflicto con las redirecciones HTTP→HTTPS de Netlify.
# El panel /admin/ funciona correctamente accediendo directamente a la URL completa.
